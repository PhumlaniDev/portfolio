name: Frontend CI/CD

on:
  push:
    branches:
      - main
      - develop
      - nav-bar
  pull_request:
    branches:
      - main

jobs:
  install-and-build:
    name: Install Dependencies & Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      - name: Run Prettier Fix
        run: npx prettier --write "src/**/*.ts"

      - name: Run Prettier Check
        run: npx prettier --check "src/**/*.ts"

      - name: Build Project
        run: npm run build --if-present

  lint:
    name: Lint Code
    runs-on: ubuntu-latest
    needs: install-and-build
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      - name: Run Linter
        run: npm run lint

  # test:
  #   name: Run Unit Tests & Coverage
  #   runs-on: ubuntu-latest
  #   needs: install-and-build
  #   steps:
  #     - name: Checkout Code
  #       uses: actions/checkout@v4

  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: 20
  #         cache: 'npm'

  #     - name: Install Dependencies
  #       run: npm ci

  #     - name: Run Tests & Generate Coverage
  #       run: npm test -- --watch=false --code-coverage

  #     - name: Upload Coverage to Coveralls
  #       run: cat coverage/lcov.info | npx coveralls --repo-token ${{ secrets.COVERALLS_REPO_TOKEN }}

  docker:
    name: Build & Push Docker Image
    runs-on: ubuntu-latest
    needs: [install-and-build, lint]
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - name: Build & Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/phumlani_arendse-portfolio:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/phumlani_arendse-portfolio:${{ github.run_number }}

  # deploy:
  #   name: Deploy to GitHub Pages
  #   runs-on: ubuntu-latest
  #   needs: [install-and-build, lint, test, docker]
  #   if: github.ref == 'refs/heads/main'
  #   steps:
  #     - name: Checkout Code
  #       uses: actions/checkout@v4

  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: 20
  #         cache: 'npm'

  #     - name: Install Dependencies
  #       run: npm ci

  #     - name: Build Project
  #       run: npm run build --prod

  #     - name: Deploy to GitHub Pages
  #       uses: JamesIves/github-pages-deploy-action@v4
  #       with:
  #         branch: gh-pages
  #         folder: dist/YOUR_PROJECT_NAME
